---
title: "Modelos Lineales Mixtos"
author: "Jorge Luis Villa Tochon"
format: html
---

#Pasos para analisis de datos 
1. Crear mi diseño experimental => modelo estadistico
2. Colectar los datos
3. Importar y verificar los factores
4. Analisis de Variancia (aov() o lm())
5. Comparación de medias
6. Gráfico

#Librerias
```{r}
library(tidyverse)
```

```{r}
library(googlesheets4)
library(lme4)
```



```{r}
library(emmeans)
```



#Cargar base de datos 


```{r}
library(googlesheets4)
library(tidyverse)

url <- "https://docs.google.com/spreadsheets/d/15r7ZwcZZHbEgltlF6gSFvCTFA-CFzVBWwg3mFlRyKPs/edit?gid=172957346#gid=172957346"

gs <- url %>%
  as_sheets_id()

fb <- gs %>%
  range_read(sheet = "fb")

view(fb)
```


```{r}
str(fb)
```



#Modelo 
$$ wue_(tuber) = \mu + bloque + riego +geno + riego*geno + e $$


#Analisis de variancia
```{r}
md <- lmer(formula = twue ~ 0 + (1|bloque) + riego*geno
           , data = fb)

anova(md)
```


```{r}
library(car)
```

```{r}
Anova(md)
```


```{r}
library(lme4)
library(emmeans)
library(multcomp)
```

```{r}
library(multcompView)
library(ggplot2)
library(dplyr)

# Modelo mixto
md <- lmer(twue ~ 0 + (1|bloque) + riego*geno, data = fb)

# ANOVA tipo II o III según interés
library(car)
Anova(md, type = 3)
```


```{r}
# Medias ajustadas para la interacción riego:geno
emm <- emmeans(md, ~ riego:geno)

# Comparación de medias con ajuste de Tukey
comp <- pairs(emm, adjust = "tukey")

# Generación de letras de significancia
letras <- multcomp::cld(emm, adjust = "tukey", Letters = letters)
```


```{r}
letras
```


```{r}
# Limpieza del data frame con letras
letras_df <- letras %>%
  as.data.frame() %>%
  mutate(riego = factor(riego, levels = unique(fb$riego)),
         geno = factor(geno, levels = unique(fb$geno)))
```


```{r}
ggplot(letras_df, aes(x = geno, y = emmean, fill = riego)) +
  geom_col(position = position_dodge(width = 0.8)) +
  geom_errorbar(aes(ymin = emmean - SE, ymax = emmean + SE),
                width = 0.2, position = position_dodge(width = 0.8)) +
  geom_text(aes(label = .group, y = emmean + SE + 0.05),
            position = position_dodge(width = 0.8),
            size = 3, vjust = 0, angle = 90) +
  labs(x = "Genotipos", y = "TWUE (eficiencia del uso del agua total)",
       fill = "Riego") +
  theme_classic(base_size = 14) +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```



```{r}
library(inti)
```

```{r}
letras_df %>% 
  plot_smr(x = "geno", y = "emmean", group = "riego"
           , error = "SE", sig = ".group")
```


```{r}
rm(list = ls())
```

```{r}
install.packages("ggplot2")
install.packages("dplyr")

library(ggplot2)
library(dplyr)

```


```{r}
Tratamiento <- rep(c("T1", "T2", "T3"), each = 4)
Rendimiento <- c(12, 15, 14, 13, 18, 17, 16, 19, 20, 22, 21, 23)
datos <- data.frame(Tratamiento, Rendimiento)
```


```{r}
head(datos)

```


```{r}
resumen <- datos %>%
  group_by(Tratamiento) %>%
  summarise(
    media = mean(Rendimiento, na.rm = TRUE),
    se = sd(Rendimiento, na.rm = TRUE) / sqrt(n())
  )

# Verifica que se haya creado
print(resumen)

```





